export const keyPassThroughProxy = new Proxy({}, { get: (_, key) => key });
export const abortTx = (tx) => {
    try {
        if (tx.error)
            return;
        tx.abort();
    }
    catch { }
};
