"use strict";
function get(value, path) {
  const segments = path.split(/[.[\]]/g);
  if (segments[0] === "")
    segments.shift();
  if (segments.at(-1) === "")
    segments.pop();
  let current = value;
  for (const key of segments) {
    if (current == null || key.trim() === "")
      return;
    current = current[key];
  }
  if (current === void 0)
    return;
  return current;
}
module.exports = get;
